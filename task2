def видалити_не_букви(слово):
    тільки_букви = [символ for символ in слово if символ.isalpha()]
    оброблене_слово = ''.join(тільки_букви)
    return оброблене_слово

введене_слово = input("Введіть слово: ")
результат = видалити_не_букви(введене_слово)
print("Результат обробки:", результат)
